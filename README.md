# okr-dashboards
Hopsworks Superset Dashboards with OKRs

Run `./okrs.sh` in the Hopsworks Terminal to answer the setup questions; it writes `okrs.md` to your home directory, which the coding agent reads when it follows CLAUDE.md (AGENTS.md is the same file, for agents that read that name).
